<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.fitmate.model.dao.CommunityCommentDao">
	<select id="selectAll">
		SELECT id, community_id AS communityId, content, writer, reg_date AS regDate, parent
		FROM community_comment
	</select>

	<select id="selectNowPost" parameterType="int">
		SELECT id, community_id AS communityId, content, writer, reg_date AS regDate, parent
		FROM community_comment
		WHERE community_id = #{id}
	</select>
	
	<insert id="insertComment" parameterType="CommunityComment">
		INSERT INTO community_comment (community_id, writer, content, reg_date, parent)
		VALUES (#{communityId}, #{writer}, #{content}, #{regDate}, #{parent})
	</insert>
	
	<update id="updateComment" parameterType="CommunityComment">
		UPDATE community_comment
		SET content = #{content}
		WHERE id = #{id}
	</update>
	
	<delete id="deleteComment" parameterType="int">
		DELETE FROM community_comment
		WHERE id = #{id}
	</delete>
	
	<delete id="deleteChildComment" parameterType="int">
		DELETE FROM community_comment
		WHERE parent = #{id}
	</delete>
	
</mapper>