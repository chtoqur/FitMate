<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- 네임스페이스에 DAO 풀패키지명을 작성 -->
<mapper namespace="com.ssafy.fitmate.model.dao.UserDao">
	<select id="selectAll">
		SELECT id, name, nickname, email, age, fitness_level AS fitnessLevel, post_code AS postCode, address, liked_videos AS likedVideos
		FROM users
	</select>

	<insert id="insertUser" parameterType="User">
		INSERT INTO users
		VALUES (#{id}, #{password}, #{name}, #{nickname}, #{email}, #{age}, #{fitnessLevel}, #{postCode}, #{address}, #{likedVideos})
	</insert>

	<select id="selectOne" parameterType="Map" resultType="User">
		SELECT id, name, nickname, email, age, fitness_level AS fitnessLevel, post_code AS postCode, address, liked_videos AS likedVideos
		FROM users
		WHERE id = #{id} AND password = #{password}
	</select>
	
</mapper>